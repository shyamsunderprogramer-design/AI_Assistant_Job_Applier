
# Core
# Dev
# Phase 2 (Excel) — installed now so the env is stable across phases
# Phase 3 (resume tailoring)
# Phase 4 (submission) — browser automation, unused in Phase 1
PyYAML>=6.0
SQLAlchemy>=2.0
anthropic>=0.40
flask>=3.1
openpyxl>=3.1
pandas>=2.2
playwright>=1.48
pypdf
pypdf>=5.0
pytest>=8.0
python-docx>=1.1
python-dotenv>=1.0
reportlab
requests>=2.32
